# ADR 0001 — Static browser application

OrthoVerba ships as a Vite static SPA. A backend, server-side rendering, cloud function, and account system are rejected because they add cost and privacy surface without improving the core known-script position problem.
