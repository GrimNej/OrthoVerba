# Browser support

Voice tracking is optimized for current Chromium-based browsers that expose Web Speech recognition, Web Workers, AudioWorklet, IndexedDB, and Intl APIs. Chrome and Edge are the primary supported voice browsers. Unsupported browsers retain the editor and manual reader.
