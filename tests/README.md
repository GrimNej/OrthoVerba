# Tests

The packaged no-browser verification suite is `npm test`, which checks the presence of the critical tracker, restart, N-best, result-slot, and lazy-persistence contracts. TypeScript compilation checks all runtime projects. The complete deterministic replay and real-browser matrix described by the blueprint should be expanded as the product is tuned against recorded recognition fixtures and physical devices.
